{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
      
    {{/each}}

</section>
{{/if}}

<button type="submit" class="btnClass"><a class="link" href="/dashboard/new"> + New Post</a></button>

